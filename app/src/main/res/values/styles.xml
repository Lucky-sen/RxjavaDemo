<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 分享弹出框动画效果设置 -->
    <style name="ChooseAnimation" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_dialog_exit</item>
    </style>

    <style name="ChooseDialog" parent="myTheme">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!--弹出框动画-->
        <item name="android:windowAnimationStyle">@style/ChooseAnimation</item>
    </style>
</resources>